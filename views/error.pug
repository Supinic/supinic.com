doctype html
html
	head
		block topheader
		include ./header.pug
		include ./navigation.pug

		- if (typeof extraScript !== "undefined") {
		script.
			!{extraScript}
		- }
	body
		block navigation
		center
			div(style="color: white; font-size: 24px;") #{message}
			div #{error}